# =======================================================================
# ENVIRONMENT CONFIGURATION TEMPLATE
# =======================================================================
# This file documents required environment variables for this project
#
# ⚠️  IMPORTANT: Placeholders shown here are for DOCUMENTATION ONLY
#
# The actual .env file MUST contain REAL values, never placeholders
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env:  cp .env.example .env
# 2. Replace ALL placeholder values with real credentials
# 3. Generate secrets using: openssl rand -base64 32
# 4. Never commit the actual .env file
# =======================================================================

# ---------------------------------------------------------------------
# Database Configuration
# ---------------------------------------------------------------------
DATABASE_URL=postgresql://username:password@host:5432/database_name
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=your_database_name
DATABASE_USER=your_database_user
DATABASE_PASSWORD=your_database_password

# SQL Server Connection (for ASP.NET/IIS projects)
SQL_SERVER_CONNECTION=Server=localhost;Database=your_database;User Id=your_user;Password=your_password;

# MySQL Connection (for PHP/Laravel projects)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=your_database
MYSQL_USER=your_user
MYSQL_PASSWORD=your_password

# ---------------------------------------------------------------------
# Application Secrets
# ---------------------------------------------------------------------
# Generate with: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_here
API_KEY=your_api_key_here
SECRET_KEY=your_secret_key_here
ENCRYPTION_KEY=your_encryption_key_here
SESSION_SECRET=your_session_secret_here

# ---------------------------------------------------------------------
# SMTP/Email Configuration
# ---------------------------------------------------------------------
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASSWORD=your_smtp_password
MAIL_FROM_ADDRESS=noreply@yourdomain.com
MAIL_FROM_NAME=Your Application Name

# ---------------------------------------------------------------------
# Test Environment Variables
# ---------------------------------------------------------------------
# These are used by test suites and must have REAL generated values
# Generate test credentials with: openssl rand -base64 16

TEST_DATABASE_URL=postgresql://testuser:testpassword@localhost:5432/test_database
TEST_DATABASE_HOST=localhost
TEST_DATABASE_PORT=5432
TEST_DATABASE_NAME=test_database
TEST_DATABASE_USER=testuser
TEST_DATABASE_PASSWORD=testpassword

TEST_API_KEY=test_api_key_here
TEST_JWT_SECRET=test_jwt_secret_here
TEST_SECRET_KEY=test_secret_key_here

# SQL Server Test Connection
TEST_SQL_SERVER_CONNECTION=Server=localhost;Database=test_db;User Id=test_user;Password=test_password;

# MySQL Test Connection
TEST_MYSQL_DATABASE=test_database
TEST_MYSQL_USER=test_user
TEST_MYSQL_PASSWORD=test_password

# ---------------------------------------------------------------------
# External API Keys
# ---------------------------------------------------------------------
STRIPE_API_KEY=your_stripe_api_key
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret

AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=us-east-1
AWS_BUCKET=your_s3_bucket_name

GOOGLE_API_KEY=your_google_api_key
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# ---------------------------------------------------------------------
# Application Configuration
# ---------------------------------------------------------------------
NODE_ENV=development
ASPNETCORE_ENVIRONMENT=Development
DJANGO_SETTINGS_MODULE=yourproject.settings.development
RAILS_ENV=development

DEBUG=true
LOG_LEVEL=info
PORT=3000

# ---------------------------------------------------------------------
# Redis Configuration (if using caching)
# ---------------------------------------------------------------------
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

# ---------------------------------------------------------------------
# Web Server Configuration
# ---------------------------------------------------------------------
# Used by Nginx/Apache for environment-specific settings
SERVER_NAME=yourdomain.com
SSL_CERTIFICATE_PATH=/path/to/ssl/cert.pem
SSL_CERTIFICATE_KEY_PATH=/path/to/ssl/key.pem

# ---------------------------------------------------------------------
# Selenium/E2E Testing Configuration
# ---------------------------------------------------------------------
SELENIUM_GRID_URL=http://localhost:4444/wd/hub
SELENIUM_BROWSER=chrome
SELENIUM_HEADLESS=true
E2E_BASE_URL=http://localhost:3000

# ---------------------------------------------------------------------
# Security Configuration
# ---------------------------------------------------------------------
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com
TRUSTED_PROXIES=127.0.0.1
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=60000

# ---------------------------------------------------------------------
# Monitoring & Logging
# ---------------------------------------------------------------------
SENTRY_DSN=your_sentry_dsn
SENTRY_ENVIRONMENT=development

LOG_FILE_PATH=/var/log/yourapp/app.log
LOG_MAX_SIZE=10m
LOG_MAX_FILES=5

# =======================================================================
# SECURITY REMINDERS
# =======================================================================
# ✅ Generate secrets with: openssl rand -base64 32
# ✅ Use different values for dev/test/prod environments
# ✅ Never commit the actual .env file (add to .gitignore)
# ✅ Rotate secrets periodically (every 90 days)
# ✅ Use environment variables even in test code
# ✅ Test environment variables should be randomly generated, not "test123"
#
# 🔒 The .env file should contain REAL values only - NO PLACEHOLDERS
# =======================================================================
